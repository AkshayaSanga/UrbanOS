"use client";
import { useState } from "react"; import { api } from "@/lib/api";
export default function Register(){ const [form,setForm]=useState({full_name:"",email:"",password:"Citizen@123",role:"citizen"}); async function submit(e:React.FormEvent){e.preventDefault(); await api.post('/auth/register',form); location.href='/login'} return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-mvborder bg-mvcard p-8"><h1 className="text-2xl font-bold">Citizen Registration</h1>{['full_name','email','password'].map(k=><input key={k} type={k==='password'?'password':'text'} placeholder={k.replace('_',' ')} className="mt-4 w-full rounded-xl border border-mvborder bg-slate-950 p-3" value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>) }<button className="mt-6 w-full rounded-xl bg-mvblue p-3 font-semibold text-slate-950">Register</button></form></main> }
