"use client";
import { useEffect, useState } from "react"; import DashboardLayout from "@/components/layout/DashboardLayout"; import { api } from "@/lib/api"; import type { Asset } from "@/types";
export default function Assets(){ const [items,setItems]=useState<Asset[]>([]); useEffect(()=>{api.get('/assets').then(r=>setItems(r.data))},[]); return <DashboardLayout><h1 className="text-2xl font-bold">Asset Management</h1><p className="mb-6 text-mvmuted">Street lights, traffic signals, CCTV, water pumps and public buildings.</p><div className="overflow-hidden rounded-2xl border border-mvborder"><table className="w-full text-sm"><thead className="bg-mvcard text-mvmuted"><tr><th className="p-3 text-left">Asset</th><th>Type</th><th>Status</th><th>Address</th><th>Next Maintenance</th></tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-t border-mvborder"><td className="p-3 font-medium">{i.name}</td><td>{i.asset_type}</td><td>{i.status}</td><td>{i.address}</td><td>{i.next_maintenance_date || '-'}</td></tr>)}</tbody></table></div></DashboardLayout> }
