"use client";
import { useEffect, useState } from "react"; import Link from "next/link"; import DashboardLayout from "@/components/layout/DashboardLayout"; import { api } from "@/lib/api"; import type { Complaint } from "@/types";
export default function Complaints(){ const [items,setItems]=useState<Complaint[]>([]); const [status,setStatus]=useState(""); useEffect(()=>{api.get('/complaints',{params:{status:status||undefined}}).then(r=>setItems(r.data))},[status]); return <DashboardLayout><div className="mb-6 flex items-center justify-between"><div><h1 className="text-2xl font-bold">Complaints</h1><p className="text-mvmuted">Manage citizen complaints and officer assignment workflow.</p></div><Link href="/complaints/new" className="rounded-xl bg-mvblue px-4 py-2 font-semibold text-slate-950">New Complaint</Link></div><div className="mb-4"><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border border-mvborder bg-mvcard p-3"><option value="">All Status</option><option>Submitted</option><option>Assigned</option><option>In Progress</option><option>Resolved</option><option>Rejected</option></select></div><div className="overflow-hidden rounded-2xl border border-mvborder"><table className="w-full text-sm"><thead className="bg-mvcard text-mvmuted"><tr><th className="p-3 text-left">ID</th><th className="p-3 text-left">Title</th><th>Category</th><th>Status</th><th>Priority</th><th>Address</th></tr></thead><tbody>{items.map(i=><tr key={i.id} className="border-t border-mvborder"><td className="p-3">#{i.id}</td><td className="p-3 font-medium">{i.title}</td><td>{i.category}</td><td><span className="rounded-full border border-mvborder px-2 py-1">{i.status}</span></td><td>{i.priority}</td><td>{i.address}</td></tr>)}{!items.length&&<tr><td colSpan={6} className="p-8 text-center text-mvmuted">No complaints found.</td></tr>}</tbody></table></div></DashboardLayout> }
