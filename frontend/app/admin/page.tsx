"use client";
import { useEffect, useState } from "react"; import DashboardLayout from "@/components/layout/DashboardLayout"; import { api } from "@/lib/api";
export default function Admin(){ const [deps,setDeps]=useState<any[]>([]); useEffect(()=>{api.get('/departments').then(r=>setDeps(r.data))},[]); return <DashboardLayout><h1 className="text-2xl font-bold">Admin Console</h1><p className="mb-6 text-mvmuted">Department overview, governance workflows and assignment control.</p><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{deps.map(d=><div key={d.id} className="rounded-2xl border border-mvborder bg-mvcard p-5"><h2 className="font-semibold">{d.name}</h2><p className="mt-2 text-sm text-mvmuted">{d.description}</p></div>)}</div><section className="mt-6 rounded-2xl border border-mvborder bg-mvcard p-5"><h2 className="font-semibold">Audit Logs</h2><p className="mt-2 text-sm text-mvmuted">Backend audit logging is enabled for login, complaint, asset and alert actions. Extend this page by adding `/audit-logs` route for a production admin view.</p></section></DashboardLayout> }
