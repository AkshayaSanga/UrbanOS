"use client";
import { useEffect, useState } from "react"; import DashboardLayout from "@/components/layout/DashboardLayout"; import { api } from "@/lib/api"; import type { Alert } from "@/types";
export default function Alerts(){ const [items,setItems]=useState<Alert[]>([]); useEffect(()=>{api.get('/alerts').then(r=>setItems(r.data))},[]); return <DashboardLayout><h1 className="text-2xl font-bold">Emergency Alerts</h1><p className="mb-6 text-mvmuted">Flood, fire, accident, road closure and public disruption alerts.</p><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(i=><div key={i.id} className="rounded-2xl border border-mvborder bg-mvcard p-5"><div className="flex justify-between"><h2 className="font-semibold">{i.title}</h2><span className="rounded-full border border-mvborder px-2 py-1 text-xs">{i.severity}</span></div><p className="mt-3 text-sm text-mvmuted">{i.message}</p><p className="mt-4 text-sm">{i.location_name}</p></div>)}</div></DashboardLayout> }
